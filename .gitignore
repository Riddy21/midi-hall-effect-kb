# PlatformIO — build output, toolchain staging, downloaded libs (`.pio/` is sufficient)
.pio/

# Editors / local IDE state
.vscode/
.idea/

# LSP / generated compile DB (regenerated by `pio run` when needed)
compile_commands.json
.cache/

# Local Python tooling (often used beside PlatformIO)
.venv/
venv/
__pycache__/
*.py[cod]

# OS
.DS_Store
